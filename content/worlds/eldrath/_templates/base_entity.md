---
schema_version: "{{schema_version}}"
id: "{{entity_id}}"
name: "{{entity_name}}"
content_status: "{{content_status}}"
tags:
  - "{{tag}}"
aliases:
  - "{{alias}}"
summary: "{{summary}}"
references:
  - "{{reference_id}}"
art:
  - "{{asset_path}}"
notes: "{{editorial_notes}}"
---

> **Aviso de template:** este arquivo não é conteúdo real. Copie-o para um template especializado antes de criar uma entidade. Placeholders nunca podem chegar a um arquivo com `content_status: approved`.

# Resumo

{{summary_text}}

## Contexto

{{context_text}}

## Observações editoriais

{{editorial_notes_text}}
